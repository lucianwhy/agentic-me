# Production Environment Configuration

environment: "production"
debug: false

logging:
  level: "INFO"

security:
  secure_cookies: true

rate_limit:
  requests_per_minute: 20
  rate_limit_ms: 3000

llm:
  timeout: 30

# 默认公开访问。如需邀请码，取消注释并填写：
# invite_codes:
#   PROD2024:
#     company: "某公司招聘"
#     recruiter: "招聘负责人"
#     active: true
invite_codes: {}
